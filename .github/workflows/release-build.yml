# SPDX-FileCopyrightText: 2026 Alin-Petru Roșu <rosualinpetru@gmail.com>
# SPDX-License-Identifier: Apache-2.0
# Builds the sdist and the wheels, at a release version or the commit.
name: Release Build

on:
  workflow_call:
    inputs:
      interpreters:
        description: JSON array of cibuildwheel interpreter tags to build.
        type: string
        default: '["cp310", "cp311", "cp312", "cp313", "cp314"]'
      release-version:
        description: Version of the distribution; empty derives one from the tree.
        type: string
        default: ""
  workflow_dispatch:
    inputs:
      interpreters:
        description: JSON array of cibuildwheel interpreter tags to build.
        type: string
        default: '["cp310", "cp311", "cp312", "cp313", "cp314"]'
      release-version:
        description: Version of the distribution; empty derives one from the tree.
        type: string
        default: ""

permissions:
  contents: read # checkout

# Named literally because github.workflow inside a call names the caller, whose
# group this would then be: a run waiting on itself is cancelled as a deadlock.
concurrency:
  group: release-build-${{ github.ref }}
  cancel-in-progress: false

env:
  SETUPTOOLS_SCM_PRETEND_VERSION_FOR_VFHE: ${{ inputs.release-version || format('0+g{0}', github.sha) }}

jobs:
  # ----------------------------------------------------------------- sdist ---

  sdist:
    name: Sdist
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      PIP_CONSTRAINT: ci-constraints-requirements.txt
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          submodules: recursive
      - name: Set up dev environment
        uses: ./.github/actions/setup-dev
        with:
          groups: release
          pip-cache: "false"
      - name: Build sdist
        run: make sdist
      - name: Check package metadata
        run: twine check --strict dist/*.tar.gz
      - name: Upload sdist artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          if-no-files-found: error
          name: dist-sdist
          overwrite: true
          path: dist/*.tar.gz
          retention-days: 7

  # ---------------------------------------------------------------- wheels ---

  wheels:
    name: Wheels (${{ matrix.os }}, ${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [macos-26, ubuntu-24.04, ubuntu-24.04-arm]
        python: ${{ fromJSON(inputs.interpreters) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          submodules: recursive
      - name: Build wheels
        uses: pypa/cibuildwheel@4726cd35bb13f7bde50cf2761f2499ac7b3aa32c # v4.1.1
        with:
          output-dir: dist
        env:
          CIBW_BUILD: ${{ matrix.python }}-*
          # The manylinux build runs in a container, which inherits nothing.
          CIBW_ENVIRONMENT_PASS_LINUX: SETUPTOOLS_SCM_PRETEND_VERSION_FOR_VFHE
      - name: Set up dev environment
        uses: ./.github/actions/setup-dev
        with:
          groups: publish
      - name: Embed SBOM
        run: make sbom-embed
      - name: Check package metadata
        run: twine check --strict dist/*.whl
      - name: Upload wheels
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          if-no-files-found: error
          name: dist-wheels-${{ matrix.os }}-${{ matrix.python }}
          overwrite: true
          path: dist/*.whl
          retention-days: 7
